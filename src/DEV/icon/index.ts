import { TStateIcon } from 'src/common.types';
import { TBootstrapState } from 'src/state/_state.common.types';

const dev_bootstrap_icons_state = {

// TODO - Insert icon states required for the development UI.
//        e.g. If you're building a developer page and you need to use
//             specific icons, you can define them here.

} as TBootstrapState<TStateIcon>;

export default dev_bootstrap_icons_state;