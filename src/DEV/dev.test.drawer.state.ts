import { TStatePageDrawer } from '../shared';

const devTestDrawer: TStatePageDrawer = {
  '_type': 'mini',
  'items': [
    {
      'type': 'text',
      'has': {
        'text': 'Hello world!'
      }
    }
  ],
};

export default devTestDrawer;